<?xml version="1.0" encoding="utf-8"?>
<!--
    AndroidManifest.xml — app entry point, permissions, and feature declarations

    BLE permission strategy (Android changed this in API 31):
      API <= 30: BLUETOOTH + BLUETOOTH_ADMIN + ACCESS_FINE_LOCATION
                 (location was required for BLE scanning on older Android)
      API >= 31: BLUETOOTH_SCAN (neverForLocation) + BLUETOOTH_CONNECT
                 (location no longer required if neverForLocation is set)
    Both sets are runtime-checked in MainActivity.getRequiredPermissions().

    android.hardware.bluetooth_le is marked required=true — the app will
    not appear in the Play Store on devices without BLE hardware.

    Related:
      MainActivity.kt     — runtime permission checking and scan trigger
      android/notes/ble.md — BLE permission notes and Android version caveats
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Bluetooth permissions for API <= 30 -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30" />

    <!-- Bluetooth permissions for API 31+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.RefinePS">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RefinePS">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
